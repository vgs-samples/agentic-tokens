services:
  server:
    build: ./server
    ports:
      - "3000:3000"
    env_file:
      - .env
  caddy:
    image: caddy:2-alpine
    ports:
      - "4200:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./client:/srv:ro
      - caddy_data:/data
volumes:
  caddy_data:
